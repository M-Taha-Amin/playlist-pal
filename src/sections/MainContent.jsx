import { SearchBar, SearchResults } from '../components';

const MainContent = () => {
  return (
    <main>
      <SearchBar />
      <SearchResults />
    </main>
  );
};
export default MainContent;
